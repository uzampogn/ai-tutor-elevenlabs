# Fixtures
Golden fixtures backing the contracts. The RAG/eval golden set already lives in
`eval/dataset.json` + `eval/baseline.json` (committed, frozen — alert rule A4);
it is referenced, not duplicated. Add new fixtures here when a contract needs one.
