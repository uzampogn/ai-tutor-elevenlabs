# Backlog

> **Generated from beads — do not edit by hand.** Beads is the source of truth
> for status, dependencies and the `Owns` file boundary; this file is the
> human-readable view of it, regenerated by the orchestrator or a design session
> in the same step that changes beads. To change the backlog, run
> `/design-session` or `bd` directly — never edit this file. Hand edits are lost
> on the next regeneration, and a stale view breaks restartability.

**Status:** ⬜ ready · 🟡 in progress · ⛔ blocked (deps open) · 🙋 human-bound · ❄ deferred · ✅ done

Rows are in build sequence (top = earliest delivered). Rows with disjoint
`Owns` and no open deps can run as parallel lanes.

## Items

| # | Item | Outcome | Status | Owns / files | Deps | Bead |
|---|---|---|---|---|---|---|
| 1 | Bootstrap: repo skeleton + CI green + hello-world deploy | Deploy path proven on day 1 | ✅ | repo root | — | dl-001 |
| 2 | Freeze `contracts/` v1.0 (schema + API shape + repo layout) | Parallel lanes can't drift on shared surfaces | ✅ | `contracts/` | — | dl-002 |
| 3 | [00-example-metric-drawer](00-example-metric-drawer/) — 4-section drawer, rendered formula | Non-experts understand a metric before acting on it | ⬜ | `frontend/src/components/MetricDrawer.tsx` + test | 2 | dl-003 |
| 4 | Deploy credentials for staging (HUMAN-BOUND) | — | 🙋 | — | — | dl-004 |

<!-- Rows are regenerated from `bd list --json` — these 4 are shown so you know
the shape they take. Rows 1 (walking skeleton first) and 2 (contracts frozen
before parallelizing) are the recommended opening moves for a new build. -->

## How beads fits in

[beads](https://github.com/steveyegge/beads) (`bd`) is a required component of
this harness — status and dependencies that survive compaction and session death.

- **1 bead per item**, created by `/design-session`, carrying the item title,
  the **1-line user outcome** (what this buys the user — distilled from the
  observation's C line), the `Owns` file boundary, its dependencies (`bd dep`),
  and the paths to its `spec.md` + `plan.md`.
- **`bd ready` is the queue.** The orchestrator claims with
  `bd update <id> --status=in_progress` and closes with `bd close <id>`,
  regenerating this file in the same step.
- **Single writer:** the orchestrator and design sessions. Workers never run
  `bd` — they report via plan checkboxes and `../03-build-reports/`, so parallel
  worktrees never collide on the tracker.
- **Session close:** `bd export -o .beads/issues.jsonl`. The Stop hook
  (`../../.claude/hooks/stop-bead-check.sh`) blocks a session from ending with
  in-progress beads unhandled.
- **Richer view (optional):** `bv`, the beads TUI — board and dependency graph.
